- content_for :title do
	Demo of jsCSS
- content_for :head do
	%script{src: "jsCSS.min.js"}

:maruku
	# jsCSS
	
	Create a new CSS tag with <code><span class="var">yourName</span> <span class="maths">= new</span> <span class="function">jsCSS</span>();</code>. <code><span class="var">yourName</span></code> can be any variable name you like. You can create as many <code><span class="maths">new</span> <span class="function">jsCSS</span>()</code>s as you need, and the ones you make last will take priority. To include this file, simply add the following to your page's <code><span class="rule">&lt;head&gt;</span></code> tag:

%pre
	:preserve
		<span class="rule">&lt;script</span> <span class="selector">src=</span><span class="string">"jsCSS.min.js"</span><span class="rule">&gt;&lt;/script&gt;</span>

:maruku
	Add rules with <code><span class="var">yourName</span>.<span class="function">addRule</span>(<span class="var">selector</span>,<span class="var">rules</span>)</code>, where <code><span class="var">selector</span></code> is a text string, a normal CSS selector. <code><span class="var">rules</span></code> is an object containing all the rules you would like to apply as <code>{<span class="string">"key"</span>: <span class="string">"value"</span>}</code> pairs.
	
	Delete rules with <code><span class="var">yourName</span>.<span class="function">deleteRule</span>(<span class="var">selector</span>,<span class="var">rules</span>)</code>, where <code><span class="var">selector</span></code> is a text string, a normal CSS selector. <code><span class="var">rules</span></code> is an Array containing all the rules you would like to delete as strings.
	
	My name's Xsanda, and I'd love it if you were to mention it if you use this tool!
	
	### Download
	
	* [Minified version](jsCSS.min.js)
	* [Expanded version (for developers)](jsCSS.js)
	
	### Example

%pre
	:preserve
		<span class="maths">var</span> <span class="var">css</span> <span class="maths">= new</span> <span class="function">jsCSS</span>()
		<span class="var">css</span>.<span class="function">addRule</span>(<span class="string">"p, a"</span>,{
			<span class="string">"font-family"</span>: <span class="string">"'Segoe UI', Helvetica, Calibri, sans-serif"</span>
		}).<span class="function">addRule</span>(<span class="string">"code, pre"</span>,{
			<span class="string">"font-family"</span>: <span class="string">"'Ubuntu Mono', Monaco, monospace"</span>
		}).<span class="function">addRule</span>(<span class="string">"h1, h3"</span>,{
			<span class="string">"font-family"</span>: <span class="string">"'Segoe UI', Helvetica, Calibri, sans-serif"</span>,
			<span class="string">"text-decoration"</span>: <span class="string">"underline"</span>
		})

		<span class="maths">var</span> <span class="var">colouring</span> <span class="maths">= new</span> <span class="function">jsCSS</span>()
		<span class="var">colouring</span>.<span class="function">addRule</span>(<span class="string">"span.string"</span>,{
			<span class="string">"color"</span>: <span class="string">"red"</span>
		}).<span class="function">addRule</span>(<span class="string">"span.maths"</span>,{
			<span class="string">"color"</span>: <span class="string">"blue"</span>
		}).<span class="function">addRule</span>(<span class="string">"span.var"</span>,{
			<span class="string">"color"</span>: <span class="string">"purple"</span>
		}).<span class="function">addRule</span>(<span class="string">"span.function"</span>,{
			<span class="string">"color"</span>: <span class="string">"teal"</span>
		}).<span class="function">addRule</span>(<span class="string">"span.selector"</span>,{
			<span class="string">"color"</span>: <span class="string">"maroon"</span>
		}).<span class="function">addRule</span>(<span class="string">"span.rule"</span>,{
			<span class="string">"color"</span>: <span class="string">"fuchsia"</span>
		})

:javascript
	var css = new jsCSS();
	css.addRule("p,a",{
		"font-family": "'Segoe UI', Helvetica, Calibri, sans-serif"
	}).addRule("code,pre",{
		"font-family": "'Ubuntu Mono', Monaco, monospace"
	}).addRule("h1,h3",{
		"font-family": "'Segoe UI', Helvetica, Calibri, sans-serif",
		"text-decoration": "underline"
	})
	var colouring = new jsCSS();
	colouring.addRule("span.string",{
		"color": "red"
	}).addRule("span.maths",{
		"color": "blue"
	}).addRule("span.var",{
		"color": "purple"
	}).addRule("span.function",{
		"color": "teal"
	}).addRule("span.selector",{
		"color": "maroon"
	}).addRule("span.rule",{
		"color": "fuchsia"
	})